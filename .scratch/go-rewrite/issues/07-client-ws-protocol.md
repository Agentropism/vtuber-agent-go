Type: grilling
Status: resolved
Blocked by: 01 (已解决,本票现为 frontier;协议草案直接采用 01 报告第五节建议)

## Question

新前端 WebSocket 协议设计(自由度:可重设计,无需兼容旧 /client-ws):音频帧(audio base64 + volumes + display_text)、表情/动作指令、字幕与控制事件(conversation-chain-start/end、interrupt、force-new-message 等)、心跳与重连语义、静态资源(live2d-models 目录)托管路径。基于 01 的渲染方案给出协议草案(JSON 消息类型表)供用户确认。

## 进度：0%

下一步：等 01 产出后 grilling 用户确认协议草案。